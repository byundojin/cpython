[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pyarena_alloc"
version = "0.0.1"
description = "Per-interpreter memory quota via PyMem_SetAllocator hook"
requires-python = ">=3.14"
